# HTTP — payroll subgraph (federation)
PORT=3001

# PostgreSQL — separate database from employees subgraph
POSTGRES_HOST=localhost
POSTGRES_PORT=5432
POSTGRES_USER=postgres
POSTGRES_PASSWORD=postgres
POSTGRES_DB=employee_payroll
